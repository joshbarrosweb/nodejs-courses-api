import { jest, expect, test, describe } from "@jest/globals";

// import superTest from "supertest";

describe("E2E", () => {
  test.todo("GET / - should return all users");
  test.todo("GET /:id - should return a user with given id");
  test.todo("POST / - should create a new user");
  test.todo("PUT /:id - should update a user with given id");
  test.todo("DELETE /:id - should delete a user with given id");
});
